<!-- SPDX-FileCopyrightText: 2026 maninblack -->
<!-- SPDX-License-Identifier: MIT -->

# Presenter UI 2.10 integration verification

- Date: 17 September 2026
- Scope: operator-authorized real Presenter integration of accepted Mockup 2.10
- Status: implemented; automated UI regression and preserved-Test read-only smoke
- Return point: existing `checkpoint/pre-ui-20260917`; not moved or replaced
- Reference: [complete mockup](../demo/mockups/aosedge-demo-interaction-mockup-2-10.html)
- Plan: [current delivery increment](../planning/active/demo-studio-delivery-plan.md)

## Implemented boundary

| Area | Implementation and evidence |
| --- | --- |
| Vehicle summaries | Brake/Tire recent backend records, exact-current-release result and receipt time; Cloud connection and controller CPU/memory. No mock fallback and no guest reads. |
| Detail navigation | Backend and Cloud dialogs retain Vehicle/team context. Software, instance and component detail clicks remain. Team authoring stays separate. |
| Shared reads | One normal vehicle-data observer per visible team, shared with its dialog; five-second backend interval. One shared Cloud resource observer; ten-second interval. Hidden document polling pauses. Explicit diagnostic mock history retains its separate opt-in read path. |
| Identity and failure | Run/Cloud/Unit identity separates cached data and story proof. Failed reads retain labelled last-known records; a changed identity removes them. Missing values are not zero, and unknown units are not guessed. |
| Story/actions | Current installed-release durable product evidence drives the existing guide without requiring a dashboard click. Finish replaces the completed-story monitoring shortcut and retains explicit confirmation. Authoring, assignment, reset, retirement and credentials use unchanged Demo Control commands. |
| Session | Lifecycle, Cloud and Test setup sections. Opening Lifecycle does not inspect credentials or mutate the environment. Existing certificate/setup actions remain explicit. |
| Layout and accessibility | Retained B2 miniatures and actors; anchors track card/controller resizes. Larger typography on the main desktop, bounded compact layouts, paginated records and resources. Dialog Escape, keyboard trapping, nested confirmation and focus return are checked. |

No CARLA, native Driving Control/Telemetry, VM, service package, Cloud protocol,
credential-persistence or Factory image source was changed for this increment.
The accepted mockup and older artifacts were not modified by the integration.

## Automated verification

From `apps/presenter-ui`:

- `npm run typecheck`: passed.
- `npm run test:unit`: **148 passed**, including five summary/proof/resource tests.
- `npm run test:browser`: **97 passed**, including nine new summary/dialog cases.
- `npm run build`: passed; the existing local server serves the compiled assets.

Browser tests use isolated fixture endpoints, never a real provisioning,
publication, reset or Finish action. They exercise existing service
Prepare/Publish/Deploy/higher-version behavior, failure/retirement gates, popup
navigation and confirmations, exact-release proof, old records after reset,
independent backend failures, changed Test identity, Cloud resource failures,
zero DMIPS, unknown units, Session navigation, and populated layouts at
1280×720, 1728×1117 and native-panel 1118×1124. Existing 1512×982 fit checks
and paginated 20-record histories also pass. Screenshots are ignored test
artifacts under `apps/presenter-ui/test-results`, not committed telemetry.

## Preserved-Test live smoke

The already-running Presenter server was idle before asset replacement; its
session identity remained unchanged. No server, VM, CARLA or native control
restart was performed. The ordinary browser was reloaded to use the new bundle.
The native wrapper's existing guarded idle-build refresh code was not changed.

Read-only UI inspection around 20:11–20:12 UTC established:

- Cloud reports Test **Online**, Factory `.35`, VDP `78.0.0` / V3,
  Brake `58.0.0` / V3 and Tire `34.0.0` / V1, with active instance reports.
- Brake and Tire inline cards show backend-received inspection recommendations,
  with 23 and 27 recent records respectively. These are retained product results,
  not a claim of a newly performed drive or current native advisory.
- Brake and Tire detail dialogs show their own release, product data and reset
  control; closing each returns to Vehicle. Neither reset was submitted.
- Cloud's dialog shows the installed components/services and controller CPU in
  **DMIPS**. Resource scope and sample/read times remain available in details.
- Cloud's current memory projection has **no verified unit**. The UI deliberately
  shows its unchanged number plus `unit not specified`; it does not fabricate
  MiB/GiB or a percentage. This existing data-contract limitation is not closed
  by a visual change.

The live page was left on Vehicle. No new clean end-to-end drive/upgrade/Finish
cycle is claimed. Final operator review of the native desktop composition and
the next clean functional cycle remain separate acceptance steps. The native
bare executable was not addressable through the computer-use app picker;
actual live browser behavior and native-sized fixture geometry were verified,
not an independently captured native-window screenshot.
