<!-- SPDX-FileCopyrightText: 2026 maninblack -->
<!-- SPDX-License-Identifier: MIT -->

# Factory .33 build and E2E checkpoint — 13 September 2026

Status: **TEST_E2E_PASSED_WITH_EXCLUSIONS**, 13 September 09:49 UTC. Clean .33
Test passed VDP V1/V2/V3, native service replacements, backend/network recovery
and retained-identity cold stop/start. Service data is synthetic. Cloud can
still transiently misreport connectivity; the packaged client recovers without
restart. This does not qualify real KUKSA/advisory or fix Cloud event ordering.

## Scope

Build .33 with the proven latest native SM/CM fixes; exercise a clean Test,
VDP V1/V2/V3, Brake/Tire service versions, isolated backend delivery, Safe Stop,
connectivity off/on, restart and stop/start. Production is excluded. The user
reconfirmed that Cloud permissions remains broken: services use explicit
synthetic data, not KUKSA or actual vehicle advisory.

The user explicitly clarified on 13 September: **Safe Stop gates component
installation, not service installation or replacement.** Brake and Tire
replacement must be exercised in Autopilot without requesting Safe Stop.
A successful service replacement performed while already stopped does not
prove this independence.

## Completed

| Item | Evidence |
| --- | --- |
| Platform source | `f7922b02b15f6cf816f181e1bf97572b61859aea` |
| New CM patches | Exact library/app patches from the successful 13 September transient proof, now recipe inputs |
| Configuration | Final packaged CM configuration contains `idleFullStatusInterval: 60s`; code default remains disabled |
| Existing SM resources/hooks | Final package comparison passed; projector and cold/verify hooks unchanged |
| Factory native tests | Five placeholder/persistent-input/profile regressions passed on Builder |
| Local Factory tests | Two passed, including preservation of unrelated CM settings and idempotence |
| Demo Control tests | 168 passed with `PYTHONPATH=tests`; initial aggregate invocation lacked that fixture import path and did not pass |
| Offline compile/package/image QA | Passed; three nonfatal existing-category CM buildpaths warnings, no bypass |
| Disk assembly | Same six-partition layout |
| Host transfer | SHA-256 matched; immutable 0444 image |
| Catalog | Demo Control exposes .33 without catalog problems |
| Builder | Stopped cleanly; subsequent existing stop command confirmed stopped |
| Earlier VDP preparation | **19.0.0/V1**, seven paths; signed, not uploaded; subsequently found bound to .32, therefore excluded from .33 E2E |

Image beneath the operator workspace:
`demo-artifacts/aosedge-sdv-demo/factory-images/6.1.1-maninblack.33/main-qemuarm64.img`.
Size: 6,997,147,648 bytes. SHA-256:
`a302b2f2e2f238b361682ab8a529ec036ff260e00b2fb9a4d21db325d8d45761`.

VDP19 signed bundle SHA-256:
`3ea0e6abec2a55fd463dc246c045e62aa59c0023dd4281964b67dbb229dccef1`.

No image, compiled binary, runtime identity or credential entered Git. Demo
Control's .33 build binding and completed-proof retirement handling are local
source changes at this checkpoint, not a claim of a pushed CLI release.

## Previous approval boundary — resolved

Original isolated .31 comparison Test remains provisioned:

- Unit `555290f6-0cd7-4eb0-bddc-8e5cfbed7ab6`.
- System UID `2af4d5ce11244fc49bc03124acec8b65`.
- Workspace `aosedge-sdv-demo-qual-31`.
- Cloud read at `2026-09-13T05:36:44.333736Z`: provisioned and Online.

It shares Test Vehicles with canonical .32. Canonical Unit lifecycle rejects
foreign role-set members (`ROLE_UNIT_SET_HAS_OTHER_UNITS`). Publishing now also
introduces a comparison recipient rather than a clean single-Test experiment.

Launching `democtl --qualification factory31 unit deprovision test` was
**rejected by execution approval before the process started**. The reviewer
required explicit authority to retire the preserved comparison. No SDK/API
attempt occurred and no indirect workaround was attempted. The temporary CLI
expansion for comparison retirement was removed again.

The previously required decision was to authorize deprovision/delete of this exact comparison Cloud
Unit and stop its VM, retaining its disk and compact comparison evidence.
Production and immutable .31/.32 images remain untouched. The .33 build/test
request was not accepted by the reviewer as sufficient retirement authority.

At that previous checkpoint, all Test/Production processes, Cloud identities, assignments,
CARLA/Gateway and comparison artifacts are preserved. No .33 VM has yet been
created, booted or provisioned; no update was published in this checkpoint.

## Authorized clean-run progress — 13 September, 08:45 UTC

The user subsequently approved all requested retirement actions and the clean
run. Every live operation below is through `democtl`. Production, immutable
Factory originals and the isolated comparison disk remain preserved.

- Isolated .31 deprovision stopped its CM. Cloud WS confirms disconnect at
  **08:24:57.877 UTC**, code 1000. The 90-second Offline wait expired without
  starting the Cloud deprovision request. API still reports provisioned/Online
  at **08:41:19.991560 UTC**; no UMH event was found in the first bounded window.
  This is not successful retirement and no Offline guard was bypassed.
- Canonical .32 `demo retire` physically Safe-Stopped/detached the source,
  stopped CARLA/Controller, retained the prepared cleanup backends and stopped
  its CM. It is waiting for its own authoritative Offline status.
- Comparison retirement is now accepted by the existing fixed qualification
  CLI. Its deletion postcondition recognizes the exact preserved canonical
  peer instead of requiring the shared Test set to be empty. Canonical cleanup
  accepts only the pinned .31 identity already recorded as DEPROVISIONING;
  this exception does not apply to publication/provisioning, arbitrary peers,
  Production or the Cloud Offline precondition.
- 41 focused Unit/comparison tests pass. The broader suite ran **694 tests,
  OK with six skips** before the last narrowly scoped cleanup regression was
  added. Initial failures were restricted-sandbox socket/process access and a
  stale test Factory path; the corrected suite was run with local test access.
- Brake **9.0.0/V1**, **10.0.0/V2**, **11.0.0/V3** and Tire **8.0.0/V1** are
  prepared and signed, **not uploaded**. All use `DEMO_MOCK_BACKEND_ONLY`, no
  permissions. Brake source `76d80e9373c64ba815487254fe6c8820be0c22bb`;
  Tire source `fc81a37dbb68425bf659f7bbd683898d64966792`.
- VDP **20.0.0/V2** (15 read paths) is prepared, not yet signed/published.
  A sign request while canonical retirement held the writer returned
  `CURRENT_RUN_BUSY` before signing; it made no external change.

Only publication to a clean eligible Test will count as the new E2E. Local
preparation while waiting does not establish delivery, runtime or backend gates.

### Preparation correction — before publication

Existing component replay binds provenance to the current Test Factory, not
the latest catalog image. `component inspect` now exposes that existing
provenance. Both signed, unpublished VDP19/V1 and VDP20/V2 name **.32** and its
SHA; preparing them before creating .33 was an ordering mistake. They are
excluded from .33 publication, not edited in place. Create .33 first, then
allocate V1/V2/V3 successors using its actual receipt (next reserved version is
21.0.0). Release continuity remains monotonic; unused numbers are not reused.
32 component/publication regressions pass. Service packages are independent of
the Factory provenance and remain suitable for the planned clean assignment.

## Clean lifecycle and first publication — 09:04 UTC

- .31 Cloud Offline confirmed at **08:55:54.010050 UTC**. Explicit deprovision
  then completed in 10.99 seconds; deletion completed in 4.84 seconds with exact
  Unit/Node absence. Its stopped disk/logs remain in the qualification workspace.
- Canonical .32 was also Offline at **08:56:33.420380 UTC**. `demo retire`
  resumed completed phases and finished at **08:57:43.260517 UTC**. It deleted
  the Test Cloud identity, its local overlay, first-SSH key material, Test
  Factory copy/manifest and owned backend Test data; originals and Production
  remain intact. No extra CM restart was used to accelerate the Cloud queue.
- Fresh .33 VM: `e1d2fc93-c9c8-4ae7-9c7d-e98facee256a`, SSH 10022. Its
  first native password dialog was cancelled while macOS was locked, before
  VM launch. Standard terminal `vm start test` used the previously supplied
  password privately, completed in 60.78 seconds and confirmed SSH/DNS and the
  staged Test role. `demo create` resumed without a second identity or image,
  confirmed the same guest and both backends at **09:02:23.135728 UTC**.
- The user then unlocked macOS, restoring native visual-control access.
- VDP **21.0.0/V1**, seven paths, now carries the exact .33 SHA. Its signed
  bundle SHA is `648a7396a5daff2acfa26a6b79d382089d017fe94d9e54f95b4db539f89b78c2`.
  Deployment `3fb600e1-6ce1-4036-a319-9c3f845db347` returned HTTP 201 once;
  Cloud Ready confirmed at **09:04:17.471647 UTC**, version ID
  `200607d0-0026-4d11-a47b-81494635668e`. This precedes .33 provisioning.

## Native runtime and backend progress — 09:29 UTC

- First SDK provisioning completed in 32.62 seconds, Online and Test Vehicles
  membership confirmed. Unit `7e09f53d-d716-4ed3-a8b4-a8f0793c987c`, system UID
  `e1d2fc93c9c84ae79c7de98facee256a`. Production remains untouched.
- VDP21/V1 stayed pending during observed Autopilot movement; Safe Stop
  installed it at 09:09:19 UTC, slot a, PID 2170, seven paths, zero restarts.
- VDP22/V2 stayed pending while moving (Cloud read 09:17:30.980620). Safe Stop
  installed it at 09:19:08 UTC, slot b, PID 3811, 15 paths, zero restarts and
  matching committed/process slots. Public-input preparation was idempotent;
  its shared compatibility contract remains 1.0.1, not the VDP release number.
- Brake9/V1 and Tire8/V1 were published through SP, assigned using their
  separate retained Subjects and launched as native SM containers. No SM/CM
  recovery restart or runtime patch was required. Backend receipts at
  09:13:24–27 contain the fresh .33 UID and corresponding native instance IDs.
  Brake produced completed sample windows; Tire produced assessment/function
  records. All are explicitly synthetic, not actual telemetry/advisory proof.
- Brake10/V2 was published with only the Brake backend intentionally stopped.
  CM reached desired state `none` with Brake10 and both native processes alive.
  After backend start, 09:27:14 receipts include Brake10 health assessment/event
  and the queued Brake9 window completion. Existing backend data was preserved.
  This proves backend retry across replacement, but it happened in Safe Stop
  and therefore does not yet establish service independence from that gate.
- Packaged CM PID 1496 remains active with NRestarts=0; SM PID 1716 is unchanged.
  CM emitted 16 idle full-status refreshes and retained one WebSocket connection
  by the current read. This confirms the packaged recovery mechanism executes;
  it is not proof that the Cloud ordering defect is fixed.
- VDP23/V3 is prepared/signed for .33, not uploaded yet. Brake11/V3 and Tire9/V1
  are signed and reserved for service replacement in Autopilot. At 09:29 UTC
  native Driving Control showed Autopilot, LIVE telemetry, 19.4 km/h.

## Service-independent gate and VDP V3 — 09:35 UTC

No Safe Stop was requested between the 09:29 Autopilot observation and the
completed Brake/Tire replacements below. Native screenshots before and after
replacement showed LIVE Autopilot at 17.8–19.4 km/h.

| Transition | Publication | Runtime/backend result |
| --- | --- | --- |
| Brake10/V2 -> Brake11/V3 | 09:30:09.704669, deployment `c7e6e2db-735f-4870-be75-5d51b6aea7bc`, one HTTP 201 | Brake11 assessments received at 09:31:15.289 and 09:31:45.316 while driving |
| Tire8/V1 -> Tire9/V1 | 09:31:10.858774, deployment `6520d4e6-b287-4239-8aae-4ab210f42517`, one HTTP 201 | Tire9 function/assessment records at 09:32:19 after the intentionally stopped Tire backend resumed; queued Tire8 record also delivered |
| VDP22/V2 -> VDP23/V3 | 09:33:06.532099, deployment `34ef3942-622d-4d8a-b22c-ee66ab98a047`, one HTTP 201 | Cloud pending at 09:33:55.826106; guest still VDP22, desired23 in `launching`. Only after Safe Stop did VDP23 start at 09:34:44, slot a, PID 6145, 23 paths, zero restarts, process/slot match |

Brake/Tire were not assigned again or manually approved for replacement. Both
backend outages retained storage. Synthetic receipts are excluded from actual
vehicle telemetry/advisory. Tire9 is a numeric successor of functional V1, not
an invented functional Tire V2.

SM remains PID 1716, NRestarts=0, packaged binary SHA
`936fbd563f7e9d54651504f5aeba84fee0f3736861d30c2efb60eb564e039783`.
SELinux remains enforcing; complete kernel window since SM start contains zero
denials. VDP23 signed SHA:
`be5de518d343ab112a56bd13223de9d322a219107e00e42e9778019cd106b7d7`.

Cloud read at **09:35:30.715969** confirms VDP23 installed without pending/error,
but Unit status is **Connected**, previously Online. This coincides with the
known delayed-event window; exact cause is not yet correlated to Cloud logs.
No manager/VM restart is issued to hide it. Idle-refresh recovery is observed
before the next connectivity test.

## Idle recovery and external-network test — 09:41 UTC

- The Connected regression recovered to **Online at the 09:36:56.461101 API
  read**, without changing anything in the transport. CM PID 1496, zero
  restarts and one connection were unchanged. Desired state was `none`; the
  25th idle full-status refresh was recorded at 09:36:45.035493. This brackets
  automatic recovery, not the exact Cloud mutation time or its internal cause.
- `vehicle connectivity off --target test` completed at 09:37:36.201978.
  Native screenshot showed external network OFF, Autopilot 19.4 km/h and LIVE
  local telemetry. VDP23 retained PID 6145 and zero restarts. Backend receipts
  stopped advancing (latest Brake receipt 09:37:15.671 at the 09:39:09 read).
  Cloud still said Online at 09:38:08; this stale server observation is not
  treated as proof of network availability.
- `vehicle connectivity on --target test` completed at 09:40:03.955487. The
  same CM established its second connection at **09:40:05.369161**, followed
  by fresh ACK and idle full-status traffic. API confirmed Online at
  09:40:33.460048. No restart of CM, SM, VM or services was used.
- Tire9 receipts resumed by 09:40:31.908, with fresh records at 09:41:01.
  The backend stored data and native service identities were retained.

## Cold recovery and closure — 09:49 UTC

`environment park` completed at **09:43:25.052004**: physical Safe Stop, source
detach, simulator/controller stop, graceful Test VM shutdown and backend stop.
It preserved the same overlay, Factory copy, installed software, Cloud Unit,
Subjects and backend data. `environment resume` completed at **09:45:20.150504**,
restoring VM, backends, CARLA and Test connection in stationary Manual. It did
not provision again or apply a transient manager patch.

| Post-cold-start boundary | Observed result |
| --- | --- |
| Identity and Cloud | Same Unit `7e09f53d-d716-4ed3-a8b4-a8f0793c987c`; Online at 09:45:21.380530; correct Test Vehicles membership |
| VDP | 23.0.0/V3, slot a, PID 1142, 23 read paths, READY/LIVE, committed/process match, zero automatic restarts |
| Native CM | PID 1057, same packaged SHA, zero automatic restarts, one new-boot connection and two idle refreshes at first inspection |
| Native SM | PID 1120, same packaged SHA, zero automatic restarts, `/etc/aos/resources.cfg`, rootfs read-only |
| Cold public inputs | Brake/Tire metadata already present, root-owned 0444; **no manual `runtime-prepare` or `runtime-activate` after resume** |
| Native services | Same native instance identities alive; Cloud reports Brake11 and Tire9 `active`, no pending versions |
| Backend delivery | Fresh Brake11 and Tire9 receipts at 09:46:22, same .33 UID, explicit `DEMO_MOCK`, `vehicleTelemetry:false` |
| Security | SELinux enforcing; complete post-start kernel window: zero denials |
| Vehicle controls | Returned in stationary Manual/LIVE; subsequent native Autopilot observed at 19.4 km/h; final Safe Stop requested for handoff |

Final API read at **09:51:11.927665 UTC** confirms the same Test **Online**,
Brake11 and Tire9 **active**. The final native screenshot shows **Safe Stop,
0.0 km/h, LIVE telemetry and external network ON**, ready for human inspection.

The API confirms the two dedicated service Subjects remain `is_group:true`
and contain only their corresponding service assignment. Production was not
mutated in this run. Factory .31 comparison disk/logs, immutable .31/.32/.33
images and Builder caches remain preserved. The old canonical .32 disposable
Test overlay/copy and Cloud identity were removed only by the authorized clean
retirement earlier in this report. No further disk cleanup is inferred here.

### Qualification limits and follow-up

- **Not qualified:** real service KUKSA permissions, live vehicle feature
  acquisition or applied Driver Advisory. The native dashboard correctly keeps
  these advisories unavailable. Mock ingestion cannot satisfy these gates.
- **Known Cloud defect remains:** Online/Connected/Offline can lag or regress
  after delayed events. This run observed Connected -> Online self-recovery
  and a real link reconnect without restart. It is not a guarantee of exact
  recovery latency, a fix for stale-event ordering, or indefinite monitoring.
- The tested restart was graceful full stop/start of the same VM and demo
  stack, not a forced power cut or a separate guest `reboot` invocation.
- Duplicate-receipt/isolation fixtures from the unchanged backend integration
  remain the supporting offline proofs; this run adds actual native VM delivery,
  both backend outages and external-network recovery, not a second fixture
  injection path.
- Human visual acceptance and subsequent source commit/push reconciliation are
  separate from this engineering Test E2E. The Solution working tree remains
  uncommitted at this checkpoint; no clean/pushed-source claim is made.

## Executed qualification sequence

1. Resolved exact comparison retirement authority and reconciled one Demo Control
   lifecycle attempt. Never replay an uncertain external mutation.
2. Retire canonical Test .32 through `democtl demo retire`; preserve Production,
   retained service Subjects, release continuity and original images.
3. Create clean Test from existing immutable .33; **do not rebuild**.
4. Prepare/sign a new V1 **after .33 creation** and upload before provisioning, start/provision into Test
   Vehicles, connect CARLA in stationary Manual, drive then Safe Stop.
5. Confirm the new V1 installed/running; execute `service runtime-prepare test`
   before binding/assigning service Subjects. Packaged resources must permit
   first launch without `runtime-activate --restart-sm` as recovery.
6. Allocate V2/V3 successors through Demo Control. Prove pending while moving,
   installation/runtime after Safe Stop, and committed slot/process agreement
   before public-input refresh.
7. Exercise each service's first assignment and subsequent version replacement
   via SP publication, separate retained Subjects and explicit mocked data.
   Verify replacements while driving, without a Safe Stop dependency.
8. Prove backend outage/retry, connectivity off/on, reboot and stop/start;
   require native input/container recovery and persistent CM interval.
9. Observe sustained Cloud status including the delayed-event window. Client
   recovery does not fix server stale-event ordering.
10. Recorded actual E2E results and the real-KUKSA/Cloud exclusions; hand off
    human visual verification. The live evidence above, not build success alone,
    closes this scoped Test qualification.

Driving Control retains its driving session and native Autopilot/Safe Stop
controls. No session takeover or new driving protocol was implemented.
