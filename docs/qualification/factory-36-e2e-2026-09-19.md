<!-- SPDX-FileCopyrightText: 2026 maninblack -->
<!-- SPDX-License-Identifier: MIT -->

# Factory .36: clean-cycle qualification

Date: 19 September 2026. Status: DIAGNOSTIC CYCLE COMPLETED, INCLUDING FINISH.
Recorded functional gates passed; remaining defects/unexecuted branches prevent
claiming complete, uninterrupted E2E acceptance.

## Authorization and preserved scope

The operator explicitly confirmed retiring the current owned staging Test and
its working data, then creating a clean Test on Factory .36 and exercising VDP
and service updates, restart/storage retention, external network OFF/ON and
final Finish. Production, immutable .35, source, release continuity, diagnostic
reports and Builder caches are preserved. This does not restore Park/Resume in
the operator UI or authorize changes to Cloud code.

Factory selector: `6.1.1-maninblack.36/main-qemuarm64`.
Platform source: `a0f88d8fc47d5e84df874883cb01872e25516fd5`.
Image SHA-256: `9ff1377a0261028bc7583bbca09be2e2c6f55f4754988071f873cbc630ab50c6`.
The build and native regressions are recorded in
[storage-fix qualification](cm-shared-storage-fix-2026-09-19.md).
Build success is not live qualification.

Use Presenter/native controls for the visible operator flow and existing
authenticated democtl/API paths for engineering evidence. Document any CLI
bootstrap, maneuver or fault-injection exception; do not claim UI-only proof.
No synthetic telemetry or model-threshold changes are allowed.

## Preceding Test retirement

At 15:46:55 UTC the authoritative staging read confirmed Unit
`cb64acb2-12fd-4340-8962-eca8d43465f1` Online, local run
`1ac3b4f2-cfe7-4e89-88e6-974637ab7afc`, Factory .35, VDP82/V3,
Brake63/V3 and Tire37/V1. Known Brake storage-loss evidence is retained in
the earlier reports; the diagnostic Test is not represented as healthy.

One explicit Presenter Finish ran from 15:47:34.485708 to
15:48:16.474873 UTC: **41.99 seconds**, COMPLETED / RETIRED. Its receipt
confirmed stopped simulation/Test, reconciled Subjects, Cloud deprovisioning,
Unit/Node absence, scoped backend cleanup and local retirement. Presenter
returned to No controller created and empty software slots. No thirty-minute
Offline wait occurred. Deletion of the owned working data is permanent;
Factory .35, published packages and release continuity are retained.

## Clean creation checkpoint

The operator UI selected .36 and submitted Create once at
15:49:56.605370 UTC, operation `7f5b0813-06dd-4abc-9065-8cb4b1b8eb00`.
New local run: `3ba61842-c8d9-45d7-864d-af92af55d937`.
Its .36 overlay exists, but guest startup is waiting for native first-access
credentials. A read-only checkpoint confirmed the VM process STOPPED and no
registration/provisioning started. Do not count the local overlay or UI
Not provisioned label as a successful guest boot.

The computer-use tool could not address the native password dialog. Execution
review rejected cancelling that dialog to switch to terminal enrollment; no
cancellation or alternate credential path was executed. The operator was asked
to complete the normal Use once dialog. Continue this same checkpoint after
access is supplied; do not create a second run or bypass the access review.

At 15:53:02.546279 UTC the original request returned PARTIAL / start-test,
`VM_ACCESS_CANCELLED_OR_DIALOG_UNAVAILABLE`, after the prompt timeout. No
guest enrollment, provisioning or publication was performed. The checkpoint
remains available for an explicit continuation through the normal access flow.

The three focused Factory .36 source-registration/build-gate tests passed again.
Read-only metadata confirms the unchanged image is mode 444 and 6,997,147,648
bytes. No redundant full-image hash or new build was run.

### Native access completed by the operator

The operator reported no dialog and requested continuation. After reconciling
the ended request and same run, the UI Continue preparation action reopened
normal native access. The operator entered the password personally. The same
run completed Create from 15:58:09.748942 to 15:59:36.866863 UTC (87.12 s,
including human input and first boot). Enrollment used the standard serial
login/SSH-public-key/host-key verification path. No alternative credential
path, second Test or password persistence was used.

At 16:00:04 UTC, read-only guest evidence confirmed .36, Enforcing policy,
unprovisioned/inactive CM/SM/IAM with zero restarts, and no service storage yet.
The boot contains a failed NFS server and general audit/getty/sshd AVC entries;
these are recorded rather than claiming a clean audit. Their baseline scope
must be distinguished from fresh service-path AVCs during functional tests.

One UI Start simulator request was submitted at approximately 16:00:18 UTC.

## Initial component and provisioning

Detached simulation reached RUNNING_UNASSIGNED with source run
`10f54ae4-b512-437f-8df0-d4df7ba30103`, actor 25, stationary Safe Stop and
both native advisories Not available. UI prepared VDP V1 / **83.0.0** and
published it from 16:02:06.685299 to 16:02:12.833369 UTC. Cloud READY was
confirmed for deployment `ca866f3d-2bcf-4e5c-9d1e-75197470b4b5`, version
`42441e6c-39fd-4b6f-8555-0910fab14f3d`; signed SHA-256
`59c5782b024172eb41d5cffb3bcea6e20a8c5caec33fecf4f9c7674efbfa02e1`.

The first attempted Provision UI click was rejected by tool review because it
mistook Cloud Online/stationary Manual for prerequisites. Source inspection
confirmed these are post-registration outcomes, no request had started, and
the same UI action was subsequently accepted with that evidence. No guard or
alternate execution path was bypassed.

Provision completed from 16:04:04.782293 to 16:04:34.926230 UTC (30.14 s),
Unit `3df2d00e-62a4-4a52-85ef-179427becf65`. Cloud Online, protected Gateway
attachment and Test-set membership passed; source run/actor remained unchanged.
Native UI showed stationary Manual. VDP83 stayed Pending until explicit
native Safe Stop at approximately 16:04:57; Cloud then reported it installed.
Factory .36 and boot 6.1.0 were independently reported by Cloud inventory.

CM1560, SM1906 and IAM1524 were active with NRestarts=0. CM used packaged
`/usr/bin/aos_cm_app`, without the old diagnostic `/run` override. The correctly
named `aos-vehicle-data-provider.service` and KUKSA substrate were active.
The pre-provision NFS failure disappeared. Its explanation matches the accepted
[baseline](aosvm-apple-silicon-baseline.md): service storage mounts do not exist
until provisioning. General SSH audit denials and VDP getsched denials were
observed and remain explicitly recorded; zero-denial security is not claimed.

## First Brake service

Native Autopilot was selected before publication/assignment. UI prepared
Brake V1 / **64.0.0**, real KUKSA mode, and published once from
16:06:21.986278 to 16:06:32.080048 UTC. Signed SHA-256:
`bd27ecd600860fba2b4339f26aed3692fe3f181fed340a08bbd3e92145f930b3`.
UI confirmed publication READY and submitted first Deploy at about 16:07:31.
Installation and real window proof follow; upload alone is not acceptance.

Cloud/UI confirmed Brake64 active while native Autopilot was selected; no
Safe Stop was used for service assignment. A native Brake maneuver produced
window `c598f19f-1017-4fd9-a913-5cdc2411a556`, COMPLETE, 75 samples
(30 PRE / 25 ACTIVE / 20 POST), 8/8 chunks, zero missing sample indices.
The real retained-sample graph/table and source/receipt times were inspected
in the Presenter popup. V1 correctly reported advisory NOT_SUPPORTED.

### Accepted follow-up: initial authorization wording

The operator observed ACCESS_DENIED then RECEIVING and requested that this
be recorded for later correction, not changed during qualification.
Guest logs show service start at 16:07:45.521 UTC with
KUKSA_AUTH_UNAVAILABLE; the bootstrap reported READY at 16:08:04 and the
service accepted the VDP contract at 16:08:04.928. The first authorization
took about 19 seconds and recovered automatically, without restart or edits.

Source confirms the child starts before asynchronous credential issuance.
The service currently maps a missing/not-yet-issued token and real gRPC
UNAUTHENTICATED/PERMISSION_DENIED into the same KUKSA_AUTH_UNAVAILABLE /
ACCESS_DENIED observation. Therefore the observed label is not proof of a
permissions-package rejection. Current input is RECEIVING and delivery IDLE.

Later correction must distinguish initial authorization waiting, renewal and
genuine denied/invalid access at the producer/contract boundary, with consistent
backend/Presenter labels. Do not simply relabel every ACCESS_DENIED as waiting,
hide genuine failures, infer readiness from process startup or change retry,
permission or token-lifetime policy. This follow-up is open; no source change
or passing semantic acceptance is claimed here.

### Observed incomplete window: source frame order

During ordinary Autopilot, window `84a0cd9d-6d30-48ba-b445-916353527ff0`
(source start 16:12:02.424 UTC) ended INCOMPLETE_SOURCE_GAP: 30 PRE samples,
zero ACTIVE/POST, all 3/3 stored chunks durably received at 16:12:05.475.
This is source acquisition failure, not missing backend chunks. Brake recorded
MISSING_VALUE at 16:12:05.433 and recovered OPERATIONAL at 16:12:05.991.
VDP logs locate the upstream cause at 16:12:05.426:
`VISS source frame is not monotonic`; its bridge reconnected after 0.5 seconds.
VDP PID2063 and NRestarts=0 were unchanged. The precise producer/order trigger
remains to be investigated; this is not silently classified as expected.

The following ordinary-drive window `852663a1-cb0d-46d2-ac03-e39d9a2476e8`
completed with 60 samples (30 PRE / 10 ACTIVE / 20 POST), 6/6 chunks at
16:13:15.983. Recovery is proven, absence of source gaps is not. The operator
asked about the raw terminal-state wording; a clearer presentation can explain
an interrupted record while preserving its true terminal status and history.

## VDP V2 and Brake V1-to-V2 continuity

VDP84/V2 publication completed at 16:12:11 UTC. VDP83 remained installed
while Autopilot was selected; explicit native Safe Stop at approximately
16:16:35 allowed VDP84 installation without replacing Brake64. A subsequent
real maneuver produced V1 window `02e138a3-a450-47a7-ba2f-86d3df0e3f73`.
The Brake backend was deliberately stopped through existing democtl (database
preserved) to retain this window in the guest outbox. This engineering fault
injection is not claimed as a UI-only action.

Native Autopilot was selected again before Brake65/V2 publication. Publication
ran once from 16:31:00.393600 to 16:31:11.082686 UTC, signed SHA-256
`23ad6e3aa75363a12d738875b971f215f2da07865e9d3dcacc9f8d3eb8ecc0fe`.
Cloud and Presenter confirmed 65.0.0 active without another Deploy or Safe Stop.
The vehicle was observed stationary under Autopilot at the later check; this
proves mode independence, not continuous physical motion throughout replacement.
All nine stored V1 message files were byte-identical to their pre-update
hashes, including a second comparison after Cloud confirmed installation.

With the backend still stopped, a real Brake maneuver produced V2 assessment
`77774d32-229a-5230-baa4-e3ba028fff8d` at 16:32:28.771: MONITOR, score40,
wear54-to-60, 25 active / 13 straight qualified samples. Its wire-file SHA-256
was `184927a450550646044ff4134b5e91bcbf9c92108de1023ee58da847faa47f84`;
content SHA-256 `87bffd691755846cc52fea59c4a8f515acd930c044da6c17905875ca841e2a4a`.
New analytics therefore did not wait for the retained V1 queue to drain.

The existing backend was restarted with dataPreserved=true. At 16:35:14 UTC,
the V2 assessment and historical V1 windows arrived; the target V1 window had
76 samples, 8/8 chunks and COMPLETE. Current V2 function then reported RECEIVING,
WAITING/NOT_QUALIFIED, delivery IDLE/zero queued, advisory NOT_SUPPORTED. Source
times/provenance remained distinct from delayed receipt time. CM1560, SM1906
and IAM1524 remained unchanged with NRestarts=0. This does not yet prove the
separate CM/VM restart storage-cleanup gate.

The operator confirmed continuing this run diagnostically without source
changes. Initial authorization wording and the nonmonotonic source-frame issue
remain open; a repeat that invalidates evidence pauses the affected test.

## VDP V3, early Tire and Brake V3

Tire38/V1 was prepared/published and assigned through its retained dedicated
Subject to the current Test while Autopilot was selected. UI confirmed active
38.0.0 with VDP84/V2; its function report showed STALE/SOURCE_GAP and no result.
This is observed lack of local inputs, not a service-inferred VDP version.
The popup additionally said to wait for a completed driving exercise; that
generic explanatory text is misleading when the input status already says
STALE and is recorded as a deferred UI wording follow-up.

VDP85/V3 publication ran 16:39:34.517908 to 16:39:40.074730 UTC, SHA-256
`ab16db77bc80c19c7b6ac0ee0cd58e373f25386c7eda6ddbe4b4099f6edc039b`.
Cloud reported 84 installed / 85 pending while native Autopilot showed
19.5 km/h. Explicit Safe Stop then allowed 85 installation. Tire38 automatically
reported RECEIVING at 16:40:17.929 without reinstall; native Tire Monitoring
appeared, while Brake V2 correctly showed Waiting for service.

Before Brake V3, the persisted model was MONITOR/40, wear60, generation1,
producerEpoch `0748bb0d-0b69-4d18-8deb-aab5c30f7780` and last assessment
`77774d32-229a-5230-baa4-e3ba028fff8d`. Native stationary Manual was selected
before Brake66/V3 publication. Cloud/Presenter confirmed 66 active without
Safe Stop/Deploy; all those model fields were unchanged. Both native functions
then showed Monitoring. No new assessment was fabricated by the installation.
The persisted INSPECTION V2-to-V3 alternative is not independently reproduced
by this MONITOR branch; no arbitrary profile downgrade is performed to claim it.

## Live results and CM restart retention

Brake66's real maneuver produced INSPECTION_RECOMMENDED/34 at 16:44:34;
the Presenter popup showed receiving inputs, current result and CONFIRMED ACK,
and native telemetry independently showed Inspection recommended. Tire38's
turning maneuver produced assessment `3296e240-e512-5a21-82c8-9509bc60852f`,
43 valid active samples, INSPECTION_RECOMMENDED/66, confidence100, at
16:45:58.340. Its backend popup and native advisory both confirmed the warning.
The real braking at the end of this maneuver also advanced Brake to wear74 /
score26; this is a separate observed Brake assessment, not a UI inference.

A read-only SQLite comparison of the exact captured V1 event and V2 assessment
returned ten canonical-message hashes: all nine saved V1 file hashes and the
exact saved V2 wire hash, one row each. Delayed delivery retained original bytes.

Both owned backend containers were deliberately stopped with dataPreserved=true
for the restart proof. Fourteen pending product-message hashes and both models
were captured. One CM-only restart at 16:49:17 changed CM1560 to CM8703.
The journal exercised removal of obsolete Brake/component instances and
different-version cleanup. All fourteen captured messages remained byte-identical
(additional legitimate advisory refresh messages accumulated). Brake retained
wear74/score26, generation3, last assessment and producer epoch. Tire retained
its entire model and producer epoch, allowing normal advisory-sequence advances.
SM1906/IAM1524 were unchanged, NRestarts=0. Cloud returned ONLINE with Brake66
and Tire38 active. Both native warnings continued while the unavailable-backend
popup honestly displayed last-known rather than live evidence.

This is a passing CM-cleanup retention proof on .36; it does not close all E2E
gates. A subsequent direct guest reboot preserved the original fourteen
pending messages and Brake model, but bypassed host-managed runtime credential
restoration. VDP reported missing VISS client certificate and SM did not reach
stable startup. See the separate
[guest-reboot engineering check](factory-36-guest-reboot-2026-09-19.md).

The operator explicitly separated this engineering reboot check from the
continuous demo acceptance and instructed completion of the main cycle.
Recover the same Test using existing idempotent democtl startup/trust restoration;
do not introduce a boot fix, new image, credentials, reprovisioning or further
reboot. Then restore both backend containers and finish the remaining main-cycle
tests. This run is interrupted/recovered, not an unbroken continuous-run proof.

## Resumed main-cycle checks

The separate recovery restored this same Test by 17:01:20 UTC. Subsequent
network testing used unchanged CM2311, SM2352, IAM1022 and VDP2451, with
NRestarts=0 and boot ID `638c0168-d3aa-41d7-bf68-8f2820d7629b`.
These stable post-recovery counts do not erase the earlier reboot failure.

Tire Reset was submitted through its backend dialog at about 17:02:01 and
confirmed CLEAR; native Tire changed to Monitoring while Brake retained its
warning. Brake Reset followed at about 17:03:43 and confirmed CLEAR; both
native advisories were Monitoring before the disconnect. Backend history was
not deleted and no service was reinstalled for Reset.

Native External network OFF completed at 17:04:37.917779 UTC. Staging reported
OFFLINE by the 17:10 read (the exact first transition instant was not captured).
At both the initial and final offline backend snapshots, scoped receipt counts
were unchanged: Brake 93 product messages / 151 function observations, Tire
54 / 85. Their last receipts remained before OFF. Backend dialogs correctly
showed LAST KNOWN, not live vehicle input.

A real Brake maneuver followed by a real Tire maneuver ran while OFF. Both
native advisories changed back to INSPECTION_RECOMMENDED. Brake retained
wear68/score32, generation6, assessment
`c577a80b-d9c4-508c-9694-59cfe352a535`; Tire retained score67/confidence100,
assessment `3ee12291-5e90-502e-926d-d08f1c8763e0`. Both producer epochs
remained unchanged. Local analytics and advisory therefore operated beyond
the normal three-minute authorization lifetime without external connectivity.
Individual token-rotation events were not instrumented; no token content or
credential hashes were recorded.

Tire V1 / 39.0.0 was prepared and published once through Presenter while the
Unit was offline. Publication reached READY and Cloud showed 38 installed /
39 pending. Existing Subject/assignment remained unchanged. Native Manual
was selected before External network ON at approximately 17:14:50; no Safe
Stop or new Deploy was used to install this service update.

The Tire39 publication receipt ran from 17:12:45.224939 to
17:12:54.633062 UTC in the selected staging context.

All 32 captured pending product-file hashes subsequently matched exactly one
canonical backend message each: nine Brake and 23 Tire; zero missing hashes
or duplicates. Brake and Tire function observations returned to RECEIVING,
IDLE, zero queued. CM logged connection reestablishment at
17:16:20.838 UTC without a manager restart. An authoritative 17:18:16 read
confirmed the same Unit ONLINE, Brake66 and Tire39 active, no pending update
or instance error. Presenter reflected this state. The .36 native reconnect
fix passed this OFF/ON cycle without a recovery restart or thirty-minute wait.

Across Tire38 -> Tire39, the entire persistent model and producer epoch were
unchanged, except the expected monotonically advancing advisory sequence.
The warning continued locally. A new native Tire maneuver from
17:19:49.760189 to 17:20:04.365324 produced a real Tire39 assessment, score66,
confidence100, INSPECTION_RECOMMENDED, durably received at 17:20:03.556.
The popup showed current inputs and zero queued messages, not the old release's
result. Its Reset38 receipt was explicitly labeled historical.

Native Return to road was checked from stationary Manual at about 17:18:40.
It completed with on-road/stationary Manual feedback, held inputs cleared and
both warnings preserved. Read-only model comparison was unchanged except
normal advisory-sequence advance. Actual Manual off-road relocation and all
negative geometry branches were not exercised in this cycle.

A bounded kernel-journal query from 17:01:20 through 17:20 returned no
`avc:  denied` entries. This does not replace the earlier recorded boot/general
audit findings or prove that every audit transport was complete. No security
policy or source code was changed to pass these tests.

## Final Finish — completed after exact approval

The operator explicitly approved deleting this Test and its working data after
the tool-safety hold below. Fresh preflight confirmed staging Unit
`3df2d00e-62a4-4a52-85ef-179427becf65` ONLINE, Brake66/Tire39 active and no
active/uncertain Presenter operation. One normal Session Finish was submitted;
no alternate cleanup path or retry was used.

Operation `e9df8d0c-81c9-40ad-82a0-d689f530b651` ran from
17:28:48.390561 to 17:29:30.404052 UTC: **42.01 seconds**, COMPLETED.
Its observed stages confirmed simulation/VM stop, Subject reconciliation,
Cloud Offline, deprovisioning, Unit deletion, Unit/Node absence, scoped backend
cleanup and final local retirement. No thirty-minute Offline wait occurred.

Independent post-read of the selected staging OEM inventory returned zero
matches for the exact Unit ID or system UID. The exact Test overlay
`.local/demo-current/validation.qcow2` is absent, and the retained peer journal
contains no Test or current-vehicle assignment. Both owned backend container
names are absent after normal cleanup; they were not recreated by diagnostics.
Presenter returned to No controller created, empty service slots, no prepared
run candidate and Trace 0. Original .35 and .36 images are still present at
6,997,147,648 bytes each, mode444. No redundant full-image hash was performed.

This permanent removal covers the disposable Test and its scoped working data;
there is no backup/recovery of that run. Factory originals, published releases,
release continuity, source, reports and Production are preserved. The user
requested a consolidated review of remaining remarks before choosing fixes;
no product changes or new VM run start automatically. See the
[follow-up review](factory-36-follow-up-review-2026-09-19.md).

## Earlier Finish checkpoint and remaining open work

The native vehicle is stationary in Safe Stop; current Test is ONLINE with
VDP85/V3, Brake66/V3 and Tire39/V1. At approximately 17:22 the computer-use
safety review rejected the Session Finish click because it required a fresh,
exact confirmation of destructive Test cleanup. No alternative execution path
was used. Read-only Presenter reconciliation confirmed no active/uncertain
operation and no Finish job; the latest completed job remained Tire39 publish.
The operator was asked to approve retirement of Unit
`3df2d00e-62a4-4a52-85ef-179427becf65` and this run's working data, preserving
Factory .35/.36, published artifacts, release continuity, reports and Production.
This is an execution-authorization gate, not an observed product Finish defect.

Follow-up status:

- Final Finish/deprovision/delete and authoritative absence checks are now
  closed by the receipt and post-read above; the earlier tool-safety hold ended.
- Startup authorization wording: distinguish not-yet-issued token from genuine
  denied access; retain automatic recovery and honest failure reporting.
- The one VDP nonmonotonic-source-frame / incomplete-window episode recorded
  above; no zero-source-gap claim.
- Tire's generic waiting-for-exercise explanation when inputs are stale or
  absent; do not confuse this with genuinely waiting for an eligible maneuver.
- Complete transition/negative matrix, including the INSPECTION V2 -> V3
  inheritance branch and actual Manual off-road relocation. This run proved
  MONITOR inheritance, subsequent warnings, stationary Return to road and
  same-profile Tire replacement; it did not execute every branch.
- Bare guest reboot restoration, separately scoped and documented. The main
  cycle was interrupted/recovered and is not an uninterrupted-run proof.

Only qualification/planning documents changed during this resumed increment;
no product/source correction, new build, commit or push was performed. Existing
uncommitted work was preserved. `git diff --check` passed. No Factory artifact,
cache, diagnostic overlay or other data has been removed at this checkpoint.

## Required live gates

- Clean creation, bootstrap and empty Factory inventory/security.
- Publication before Provision, Online and same-scene Gateway attachment.
- VDP V1/V2/V3, explicit Safe Stop gating and service-update independence.
- Brake V1/V2/V3 and Tire V1: real inputs, version-appropriate results,
  independent Reset/CLEAR and local advisory.
- Persistent model, producer identity and pending bytes survive service
  replacement and CM cleanup; final-owner retirement still cleans up. Full guest
  reboot recovery is now a separate engineering check, not a required demo step.
- External OFF: no new backend ingress while local inputs, analytics,
  authorization renewal and advisory continue; ON recovers Cloud and drains
  queues without duplication or byte/provenance changes.
- Native Return to road and coherent Presenter/backend observations.
- Final Finish, exact owned Cloud/local absence and preserved non-targets.

Record failures and unexecuted branches explicitly. No prior run is silently
counted as acceptance of this image.
